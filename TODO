( ) Use object instead of functions when getting native locales
( ) Create an npm executable that checks for undefined keywords in the entire project directory
( ) demo-create-react-app
( ) demo-expo
( ) Rearrange language list to accomodate language codes returned from iOS