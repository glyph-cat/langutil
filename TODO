( ) A function in dev-additions that can help restructure how the dictionary is defined (byKey or byLang)
( ) Create an npm executable that checks for undefined keywords in the entire project directory
( ) Follow IETF language codes? https://datahub.io/core/language-codes#resource-ietf-language-tags
( ) npm run deploy-(patch|minor|major) --dry-run should not trigger `npm publish`
( ) expose Langutil's casing API??
( ) Optional values to be allowed as null or undefined instead of just undefined