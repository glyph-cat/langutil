import React from 'react'

export default {
  FAQ_CONTENT_001: {
    en: <><b>No.</b> Langutil only relies on JavaScript (and some TypeScript). This allows it to work just fine be it on browsers with React and on mobile with React Native.</>,
    'zh-Hans': <><b>不必。</b> Langutil 只依靠 JavaScript (和一点点 TypeScript)。也因为这样，langutil 才能在浏览器里和 React Native 里任由地操作。</>,
    'zh-Hant': <><b>不必。</b> Langutil 只依靠 JavaScript (和一點點 TypeScript)。也因為這樣，langutil 才能在瀏覽器里和 React Native 里任由地操作。</>,
  },
}
